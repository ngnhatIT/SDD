# Feature Package Template Alias

This path is retained only as a compatibility alias.

Use `../feature-package/README.md` as the canonical feature-package scaffold.
Use `../../specs/README.md` for the governing package rules.
