# Core Prompt Utilities

Use `prompts/core/` only after the task is already routed and the primary contract is known.

These prompts are stage utilities, not first-choice workflow entrypoints:

- `recover-context.md`
- `self-review.md`
- `pre-merge-audit.md`

Read them only after the canonical repository docs, selected execution contract, and governing feature package are loaded.
